{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "pnpm -C ../.. install --no-frozen-lockfile",
  "buildCommand": "rm -rf .next && pnpm -C ../.. --filter quiz run build",
  "outputDirectory": ".next",
  "framework": "nextjs"
}