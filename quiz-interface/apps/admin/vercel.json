{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "corepack enable && corepack prepare pnpm@9.15.0 --activate && pnpm install --no-frozen-lockfile --registry=https://registry.npmjs.org --fetch-retries=5 --fetch-retry-factor=2 --fetch-timeout=60000",
  "buildCommand": "rm -rf .next && pnpm --filter admin run build",
  "outputDirectory": ".next",
  "framework": "nextjs"
}
